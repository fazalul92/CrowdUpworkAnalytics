<%@ page import="edu.rit.se.crowdupworkanalytics.DBProcess"%>
<%
	/* String descr = request.getParameter("descr");
    DBProcess dbProc = new DBProcess();
    int ret = dbProc.addUseCase(descr);
    dbProc.disConnect();
    if (ret == 1) {
        response.sendRedirect("../addUseCase.jsp");
    } else {
        response.sendRedirect("../addUseCase.jsp?error=2");
    } */
%>